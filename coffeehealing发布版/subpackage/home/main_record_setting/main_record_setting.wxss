page {
  background-color: #fdf7e5;
}
.page {
    padding: 25rpx;
  }
.setting-item {
  background-color: #fff;
  border-radius: 36rpx;
  margin: 36rpx 20rpx 0 20rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}

/* 图标 */
.icon {
  width: 60rpx;
  height: 60rpx;
}

/* 文字右移，留图标空间 */
.shifted-text {
  margin-left: 20rpx;
}

/* 第一块布局 */
.first-item {
  display: flex;
  align-items: center;
}
.first-item .info {
  flex: 1;
}
.time-badge {
  background-color: #d19a74;
  color: #fff;
  font-size: 36rpx;
  font-weight: bold;
  padding: 8rpx 24rpx;
  border-radius: 40rpx;
}

/* 2-4块布局 */
.with-divider .top-row {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}
.text-area {
  display: flex;
  flex-direction: column;
}
.title {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
}
.desc {
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
}

/* 分割线与文字对齐 */
.shifted-divider {
  margin-left: 80rpx; /* 图标宽度60 + 左间距20 */
  margin-right: 0;
  height: 1px;
  background-color: #ddd;
  margin-top: 12rpx;
  margin-bottom: 12rpx;
}

/* 数值输入行 */
.value-row {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}
.value-input {
  font-size: 36rpx;
  font-weight: bold;
  text-align: center;
  flex: 1;
}
.unit {
  position: absolute;
  right: 0;
  font-size: 24rpx;
  color: #999;
}

/* 保存按钮 */
.save-btn {
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 56rpx 20rpx 28rpx 20rpx;
  background-color: #d19a74;
  border-radius: 36rpx;
  height: 80rpx;
  color: #fff;
  font-size: 28rpx;
  font-weight: bold;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}

/* 恢复按钮 */
.reset-btn {
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 28rpx 20rpx 56rpx 20rpx;
  background-color: #fff;
  border-radius: 36rpx;
  height: 80rpx;
  color: #c67c59;
  font-size: 28rpx;
  font-weight: bold;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}

/* 弹窗 */
.popup-mask {
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  display: flex;
  justify-content: center;
  align-items: flex-end;
}
.popup {
  background-color: #fff;
  border-radius: 24rpx 24rpx 0 0;
  width: 100%;
  text-align: center;
  padding: 40rpx 0;
  box-shadow: 0 -4rpx 12rpx rgba(0, 0, 0, 0.08);
  animation: slideUp 0.25s ease;
}
@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}
.popup-text {
  font-size: 32rpx;
  margin-bottom: 40rpx;
}
.popup-btn {
  width: 100%;
  padding: 20rpx 0;
  border: none;
  font-size: 32rpx;
}
.confirm {
  background-color: #c67c59;
  color: #fff;
  margin-bottom: 20rpx;
}
.cancel {
  background-color: #f5f5f5;
  color: #333;
}

/* 保存成功提示 */
.save-success-mask {
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.save-success-popup {
  background-color: #fff;
  border-radius: 24rpx;
  padding: 40rpx 60rpx;
  text-align: center;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
  animation: fadeInScale 0.3s ease;
}

@keyframes fadeInScale {
  from { 
    opacity: 0;
    transform: scale(0.8);
  }
  to { 
    opacity: 1;
    transform: scale(1);
  }
}

.success-icon {
  width: 80rpx;
  height: 80rpx;
  background-color: #4CAF50;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 40rpx;
  color: #fff;
  font-weight: bold;
  margin: 0 auto 20rpx auto;
}

.success-text {
  font-size: 32rpx;
  color: #333;
  font-weight: bold;
}