<view class="container">
  <!-- 悬浮收藏栏和自定义栏 -->
  <view class="floating-bars">
    <view class="floating-bar {{activeCategory === 'favorite' ? 'active' : ''}}" 
          bindtap="switchToFavorite">
      <view class="bar-icon">⭐</view>
      <text class="bar-text">收藏</text>
    </view>
    <view class="floating-bar {{activeCategory === 'custom' ? 'active' : ''}}" 
          bindtap="switchToCustom">
      <view class="bar-icon">⚙️</view>
      <text class="bar-text">自定义</text>
    </view>
  </view>

  <!-- 主内容区域 -->
  <view class="main-content">
    <!-- 页面标题 -->
    <view class="page-title">
      <text class="title-text">精选茶饮品牌</text>
    </view>

    <!-- 品牌卡片网格 -->
    <view class="brand-grid">
      <view wx:for="{{brands}}" wx:key="id" 
            class="brand-card" 
            bindtap="selectBrand" 
            data-brand="{{item}}">
        <view class="card-logo-container">
          <image class="brand-logo" 
                 src="{{item.logoUrl}}" 
                 mode="aspectFit" 
                 lazy-load="true"/>
        </view>
        <text class="brand-name">{{item.name}}</text>
      </view>
    </view>

    <!-- 空状态 -->
    <view wx:if="{{brands.length === 0}}" class="empty-state">
      <view class="empty-icon">📭</view>
      <text class="empty-text">暂无品牌数据</text>
    </view>
  </view>

  <!-- 收藏饮品弹窗 -->
  <view class="modal-overlay {{showFavoriteModal ? 'show' : ''}}" bindtap="hideFavoriteModal">
    <view class="modal-content" catchtap="stopPropagation">
      <view class="modal-header">
        <text class="modal-title">我的收藏</text>
        <view class="modal-close" bindtap="hideFavoriteModal">×</view>
      </view>
      
      <view class="modal-body">
        <view wx:for="{{favorites}}" wx:key="id" 
              class="favorite-item" 
              bindtap="selectFavoriteDrink" 
              data-drink="{{item}}">
          <view class="drink-icon">{{item.emoji || '☕'}}</view>
          <view class="drink-info">
            <text class="drink-name">{{item.name}}</text>
            <text class="drink-caffeine">{{item.caffeine}} mg{{item.unit || ''}}</text>
          </view>
          <view class="favorite-btn favorited" 
                catchtap="toggleFavorite" 
                data-drink-id="{{item.id}}">
            <text class="favorite-icon">★</text>
          </view>
        </view>
        
        <view wx:if="{{favorites.length === 0}}" class="empty-favorites">
          <view class="empty-icon">⭐</view>
          <text class="empty-text">暂无收藏饮品</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 自定义饮品弹窗 -->
  <view class="modal-overlay {{showCustomModal ? 'show' : ''}}" bindtap="hideCustomModal">
    <view class="modal-content" catchtap="stopPropagation">
      <view class="modal-header">
        <text class="modal-title">自定义饮品</text>
        <view class="modal-close" bindtap="hideCustomModal">×</view>
      </view>
      
      <view class="modal-body">
        <!-- 添加自定义饮品按钮 -->
        <view class="custom-add-btn" bindtap="showAddCustomModal">
          <text class="custom-add-icon">+</text>
          <text class="custom-add-text">添加自定义饮品</text>
        </view>
        
        <!-- 自定义饮品列表 -->
        <view wx:for="{{customDrinks}}" wx:key="id" 
              class="custom-item" 
              bindtap="selectCustomDrink" 
              data-drink="{{item}}">
          <view class="drink-icon">{{item.emoji || '☕'}}</view>
          <view class="drink-info">
            <text class="drink-name">{{item.name}}</text>
            <text class="drink-caffeine">{{item.caffeine}} mg{{item.unit || ''}}</text>
          </view>
          <view class="delete-btn" 
                catchtap="deleteCustomDrink" 
                data-drink-id="{{item.id}}">
            <text class="delete-icon">🗑️</text>
          </view>
        </view>
        
        <view wx:if="{{customDrinks.length === 0}}" class="empty-custom">
          <view class="empty-icon">⚙️</view>
          <text class="empty-text">暂无自定义饮品</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 添加自定义饮品弹窗 -->
  <view class="modal-overlay {{showAddCustomModal ? 'show' : ''}}" bindtap="hideAddCustomModal">
    <view class="modal-content" catchtap="stopPropagation">
      <view class="modal-header">
        <text class="modal-title">添加自定义饮品</text>
        <view class="modal-close" bindtap="hideAddCustomModal">×</view>
      </view>
      
      <view class="modal-body">
        <view class="form-item">
          <text class="form-label">饮品名称</text>
          <input class="form-input" placeholder="请输入饮品名称" 
                 value="{{customDrink.name}}" bindinput="onCustomNameInput"/>
        </view>
        
        <view class="form-item">
          <text class="form-label">咖啡因含量 (mg)</text>
          <input class="form-input" type="number" placeholder="请输入咖啡因含量" 
                 value="{{customDrink.caffeine}}" bindinput="onCustomCaffeineInput"/>
        </view>
        
        <view class="form-item">
          <text class="form-label">单位</text>
          <picker class="form-picker" range="{{unitOptions}}" 
                  value="{{customDrink.unitIndex}}" bindchange="onUnitChange">
            <view class="picker-text">{{unitOptions[customDrink.unitIndex]}}</view>
          </picker>
        </view>
      </view>
      
      <view class="modal-footer">
        <button class="btn-cancel" bindtap="hideAddCustomModal">取消</button>
        <button class="btn-confirm" bindtap="addCustomDrink">确定</button>
      </view>
    </view>
  </view>
</view>
